import { TransactionsPageContent } from "@/components/escrow/transactions-page-content";

export const metadata = {
  title: "Transactions — Escrow — FreelanceAI",
  description: "View and search escrow transaction history.",
};

export default function TransactionsPage() {
  return <TransactionsPageContent />;
}
