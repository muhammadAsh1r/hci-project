import { EscrowDashboardContent } from "@/components/escrow/escrow-dashboard-content";

export const metadata = {
  title: "Escrow Payments — FreelanceAI",
  description: "Secure escrow payment management with milestone tracking and transaction history.",
};

export default function EscrowPage() {
  return <EscrowDashboardContent />;
}
