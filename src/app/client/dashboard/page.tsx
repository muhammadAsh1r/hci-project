import { ClientDashboardContent } from "@/components/client/client-dashboard-content";

export const metadata = {
  title: "Client Dashboard — FreelanceAI",
  description:
    "Manage your projects, review proposals, hire freelancers, and track escrow payments.",
};

export default function ClientDashboardPage() {
  return <ClientDashboardContent />;
}
