import { ClientNotificationsContent } from "@/components/client/notifications/client-notifications-content";

export const metadata = {
  title: "Notifications — FreelanceAI",
};

export default function ClientNotificationsPage() {
  return <ClientNotificationsContent />;
}
