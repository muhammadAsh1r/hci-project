import { ClientTransactionsPageContent } from "@/components/client/escrow/client-transactions-page-content";

export const metadata = {
  title: "Transaction History — FreelanceAI",
};

export default function ClientEscrowTransactionsPage() {
  return <ClientTransactionsPageContent />;
}
