import { PostProjectPageClient } from "@/components/client/projects/post-project-page-client";

export const metadata = {
  title: "Post Project — FreelanceAI",
  description: "Create and publish a new project to find talented freelancers.",
};

export default function ClientPostProjectPage() {
  return <PostProjectPageClient />;
}
