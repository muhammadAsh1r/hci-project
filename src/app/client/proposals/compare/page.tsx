import { ComparePageClient } from "@/components/client/proposals/compare-page-client";

export const metadata = {
  title: "Compare Freelancers — FreelanceAI",
};

export default function ClientProposalsComparePage() {
  return <ComparePageClient />;
}
