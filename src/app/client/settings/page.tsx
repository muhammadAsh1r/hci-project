import { ClientSettingsContent } from "@/components/client/settings/client-settings-content";

export const metadata = {
  title: "Settings — FreelanceAI",
};

export default function ClientSettingsPage() {
  return <ClientSettingsContent />;
}
