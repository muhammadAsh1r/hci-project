import { ManageProjectsContent } from "@/components/client/projects/manage-projects-content";

export const metadata = {
  title: "Manage Projects — FreelanceAI",
  description: "View, filter, and manage all your posted projects.",
};

export default function ClientProjectsPage() {
  return <ManageProjectsContent />;
}
