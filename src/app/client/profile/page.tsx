import { ClientProfileContent } from "@/components/client/profile/client-profile-content";

export const metadata = {
  title: "Company Profile — FreelanceAI",
};

export default function ClientProfilePage() {
  return <ClientProfileContent />;
}
