import { ClientDashboardContent } from "@/components/client/client-dashboard-content";

export const metadata = {
  title: "Client Dashboard Demo — FreelanceAI",
  description:
    "Client dashboard demo with projects, proposals, hiring, and escrow analytics.",
};

export default function DemoClientDashboardPage() {
  return <ClientDashboardContent />;
}
