import { EscrowDashboardContent } from "@/components/escrow/escrow-dashboard-content";

export const metadata = {
  title: "Escrow Demo — FreelanceAI",
  description:
    "Escrow payments demo with milestone tracking and transaction history.",
};

export default function DemoEscrowPage() {
  return <EscrowDashboardContent />;
}
